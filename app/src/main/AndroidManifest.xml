<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.zpi.xmas2019">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".GalleryActivity">
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key"/>

        <activity
                android:name=".GMapsActivity"
                android:label="@string/title_activity_gmaps">
        </activity>
        <activity
                android:name=".HowToGetActivity"
                android:label="@string/title_activity_how_to_get"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".DriveActivity">
        </activity>
        <activity android:name=".LocationActivity">
        </activity>
        <activity android:name=".ContactActivity">
        </activity>
        <activity android:name=".RestaurantsActivity">
        </activity>
        <activity android:name=".MarketMapsActivity">
        </activity>
        <activity android:name=".EventsActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".EventsActivityMoreInf" android:parentActivityName=".EventsActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Events_ActivityItem"
                  android:parentActivityName=".EventsActivity"
                  android:label="Najlepsze wydarzenie"
        ></activity>
        <activity android:name=".Events_ActivityCalendar"
        android:theme="@style/AppTheme.PopupTheme"></activity>
    </application>

</manifest>